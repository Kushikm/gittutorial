1. Git intialization :
                      git init

2. Create files :
                      index.html( whatever files you required create all those things)

3. To see list of files:
                       ls           

4. To seee the status :
                       git status(git aware of the file but not added those  files to  the repo)

5. staging (involves adding files to the git):
                    git add filename
                    git add -all
                    git add -A

6. commit(save point):
                    git commit -m "message"

7. command help:
                  git command -help (see all available options for the specific command)
                  git help --all (see all possible commands)

8. creating new branch :
                     git branch branchname

9. branch list :
                   git branch

10. Moving from current to another branch :
                     git checkout branchname

11. merging :
              git merge branchname            

12. Accept changes

13. delete branch:
               git branch -d branchname

14. Create repo in github account

15. add this file to git:
                  git push -u origin branchname

16. add all the changes you made to github:
                    git push

17. to get updated one if anyone changed code ...to get that in our local repo:
                      git pull                    








